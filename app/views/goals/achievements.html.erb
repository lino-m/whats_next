<div class="d-flex">
  <% @achievements_pg.each do |achievement| %>
  <%#= achievement.searchable_type %>
    <%= achievement.searchable_type %>
      <%#=  achievement.title %>
    <%# else %>
      <%#= 'else' %>
    <% end %>

    <%# activity = Activity.find(achievement.activity_id) %>
    <div style='height: 50px; padding: 20px;'> <%#= achievement.title %> </div>

</div>
