<div class="container">
  <div class="row results-and-map ">

    <div class="results ">
      <h3><%= @text %></h3>
      <% @achievements.each do |goal| %>
        <%= render "shared/achievement_card_wide", achievement: goal %>
      <% end %>
    </div>

    <div class= 'g' id="map", data-markers="<%= @markers.to_json %>", data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">

  </div>
</div>




  <div style='height: 50px; padding: 20px;'></div>
