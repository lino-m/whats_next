<%= render 'shared/banner' %>

<div class="container">
  <div class="row my-5 h-100">
     <% @achievements.each do |achievement| %>
    <div class="col-3">
      <%= render 'shared/achievement_card', achievement: achievement %>
      </div>
      <% end %>
  </div>
</div>

<div class="tesst" style= "height:300px;">
  <p>test footer position</p>
</div>

